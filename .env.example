# Example environment variables for local development
# Copy this file to .env and update values before running the app

# PostgreSQL connection string (replace values with your local DB)
DATABASE_URL="postgresql://guit:guitpass@localhost:5432/guitdb?schema=public"

# Optional: separate test database for running the test suite
TEST_DATABASE_URL="postgresql://guit:guitpass@localhost:5433/guit_test?schema=public"

# App configuration
PORT=3000
NODE_ENV=development
